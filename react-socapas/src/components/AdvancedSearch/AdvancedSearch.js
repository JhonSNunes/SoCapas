import { connect } from "react-redux";
import Presentation from "components/AdvancedSearch/Presentation";
import {
	getIsOpen,
	setIsOpen
} from "components/AdvancedSearch/redux/index";

export default connect(
	( state ) => ({
	}),
	( dispatch ) => ({
	})
)( Presentation );